#include <HighBeamCamera.h>

CameraData getCameraData(void){
	CameraData data;
	//set the data according to the camera inputs
	return data;
}

bool isVehicleDetected(void){
	CameraData data = getCameraData();
	bool vehicleDetected = false;
	//decide based on the camera datas wether a vehicle is detected or not
	return vehicleDetected;
}
